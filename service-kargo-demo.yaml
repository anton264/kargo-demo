apiVersion: v1
kind: Service
metadata:
  labels:
    hej: hejsan
  name: kargo-demo
spec:
  ports:
  - nodePort: 32080
    port: 3000
    protocol: TCP
    targetPort: 80
  selector:
    app: kargo-demo
  type: NodePort
